{
  "name": "patra",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "patra": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --watch",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@patra/shared": "workspace:*",
    "commander": "^13"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22",
    "tsup": "^8",
    "typescript": "^5.8"
  }
}
