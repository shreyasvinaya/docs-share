.git
.github
.omx
.turbo
dist
node_modules
packages/*/dist
packages/server/data
packages/web/tsconfig.tsbuildinfo
*.db
*.db-shm
*.db-wal
# Exclude ALL env files from the build context so real secrets (e.g.
# .env.production) can never be baked into an image layer; keep only examples.
.env*
!.env.example
!.env.production.example
Dockerfile
docker-compose.yml
